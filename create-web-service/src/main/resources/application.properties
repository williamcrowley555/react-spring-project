spring.datasource.url=jdbc:postgresql://localhost:5432/test_database
spring.datasource.username=postgres
spring.datasource.password=admin123
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

# Port Number
server.port=8082
socketIO.server.port=81

# Host
server.host=localhost

# App Properties
william.app.jwtSecret= WilliamCrowleySecretKey
# 24 hours
william.app.jwtExpirationMs= 86400000 

# Context-path
server.servlet.context-path=/create-web-service
server.error.include-message=always

# OpenAPI Properties
module-name=Test
api-version=v1.0